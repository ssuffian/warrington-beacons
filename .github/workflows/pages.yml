name: Deploy server files to Pages

on:
  push:
    branches: [main]
    paths:
      - server/**
      - scripts/build_image_library.py
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Validate committed API contracts
        run: |
          python3 -m pip install --disable-pip-version-check -r scripts/requirements-api-contract.txt
          python3 scripts/validate_api_contract.py server/api/v1/schema.json server/api/v1/trails.json server/warrington-trails.json
      - name: Build image library
        run: |
          python3 scripts/build_image_library.py
      - uses: actions/upload-pages-artifact@v3
        with:
          path: server
      - id: deployment
        uses: actions/deploy-pages@v4
